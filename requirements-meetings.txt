# Extra dependencies for the Meetings module; use the application's existing versions when already installed.
requests>=2.31
boto3>=1.34
psycopg[binary]>=3.1
pydantic>=2.0
email-validator>=2.0
tzdata>=2024.1
# Test-only: pytest, httpx
