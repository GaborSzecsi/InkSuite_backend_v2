# Reuses the existing InkSuite auth/database/Marketing dependencies.
fastapi>=0.115,<1
pydantic>=2,<3
psycopg[binary]>=3,<4
boto3>=1.35,<2
python-multipart>=0.0.18
Pillow>=10.4
email-validator>=2,<3
cryptography>=43
# Existing auth service also requires python-jose, requests and python-dotenv.

# Protected ARC EPUB ingestion
-r requirements-arc.txt
